<view class="item" wx:for="{{taskList}}" wx:key="sn">
  <view class="title">{{item.title}}</view>
  <view class="price">{{item.formattedPrice}}</view>
  <text class="description">{{item.description}}</text>
  <view class="datetime">{{item.publishedAt}}</view>
</view>
<!-- 提示语 -->
<loading-tips loading-tip="{{loadingTip}}"></loading-tips>