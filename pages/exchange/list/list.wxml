<view class="datetime">{{publishedAt}}</view>
<view class="item" wx:for="{{ticker}}" wx:key="">
  <view class="title">{{item.currency}}</view>
  <view class="price buy">{{item.buy}}</view>
  <view class="price sell">{{item.sell}}</view>
</view>